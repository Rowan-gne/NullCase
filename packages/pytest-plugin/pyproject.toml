[project]
name = "nullcase-pytest"
version = "0.0.0"
description = "pytest plugin for NullCase (not yet implemented)"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
