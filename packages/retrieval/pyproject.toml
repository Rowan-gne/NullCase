[project]
name = "nullcase-retrieval"
version = "0.0.0"
description = "Finds existing tests to use as style examples for a target module"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
